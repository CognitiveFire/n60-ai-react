{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf dist/ && echo 'Starting build...' && npm run build && echo 'Build completed at:' $(date) && echo 'Version:' $(npm pkg get version) && echo 'Dist directory contents:' && ls -la dist/ && echo 'Current directory contents:' && ls -la ."
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
